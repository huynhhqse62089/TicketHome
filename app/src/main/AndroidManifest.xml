<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.huynhhq.tickethome">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChooseLocationActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainScreenActivity" />
        <activity android:name=".VerifyAccountActivity" />
        <activity android:name=".EventDetailActivity" />
        <activity android:name=".ChooseTicketActivity" />
        <activity android:name=".PayTicketActivity" />
        <activity android:name=".SaveEventsActivity" />
        <activity android:name=".NotifiEventActivity" />
        <activity android:name=".FinishPaymentActivity" />
        <activity android:name=".SuccessfulBookActivity"></activity>
    </application>

</manifest>